@model IEnumerable<ST10242546_CLDV6211_POE_.Models.Order>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn" style="background-color: orangered; color: white;"> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic example">
                        <a asp-action="Edit" asp-route-id="@item.OrderId" class="btn" style="background-color: orangered; color: white;">Edit</a>
                        <a asp-action="Details" asp-route-id="@item.OrderId" class="btn" style="background-color: orangered; color: white;">Details</a>
                        <a asp-action="Delete" asp-route-id="@item.OrderId" class="btn" style="background-color: orangered; color: white;">Delete</a>
                    </div>
                </td>
        </tr>
}
    </tbody>
</table>
